# Ignore most generated benchmark results (large files with trajectories)
*.jsonl
*.png
*.csv

# Track summary files (metrics only, no trajectories - ~30KB each)
!*_summary.jsonl

# Track baseline PNG plots for documentation
!baseline_*.png
